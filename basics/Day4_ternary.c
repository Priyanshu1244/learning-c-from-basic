#include <stdio.h>

int main()
{

    int a = 5, b = 7;


    // condition? expression if true : expression if false

    (a < b) ? printf("a is greator\n") : printf("b is greator");   //result is true(1) so first expression will run

    return 0;
}