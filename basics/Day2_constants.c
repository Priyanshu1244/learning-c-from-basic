#include<stdio.h>

int main(){

    const int max_driver_age = 60;    //now the value of these variables cannot be modified
    const float Pi = 3.14;  
    
    printf("the value of pi is : %f\n", Pi);
    printf("the maximum age of driver should be %d\n", max_driver_age);

    printf("__________________________________________________________________________________");
    
    
    return 0;

}